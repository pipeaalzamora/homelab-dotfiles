# =============================================================
# Stack: MEDIA
# Jellyfin · Jellyseerr · Sonarr · Radarr · Prowlarr · Bazarr · qBittorrent
# Streaming automático local para uso familiar
# =============================================================

networks:
  homelab:
    external: true

services:

  # -----------------------------------------------------------
  # Jellyfin — Servidor de streaming personal
  # Puerto: 8096
  # -----------------------------------------------------------
  jellyfin:
    image: linuxserver/jellyfin:10.10.3
    container_name: jellyfin
    restart: unless-stopped
    environment:
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
      TZ: ${TZ:-America/Santiago}
      # JELLYFIN_PublishedServerUrl: https://media.tudominio.com  # Descomentar en producción
    volumes:
      - /srv/data/jellyfin/config:/config
      - /srv/data/jellyfin/cache:/cache
      - /srv/media/movies:/data/movies:ro
      - /srv/media/series:/data/series:ro
      - /srv/media/music:/data/music:ro
    ports:
      - "8096:8096"
    # Descomentar para transcoding por GPU NVIDIA (RTX 3060/4060)
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
    networks:
      - homelab

  jellyseerr:
    image: fallenbagel/jellyseerr:2.2.3
    container_name: jellyseerr
    restart: unless-stopped
    environment:
      LOG_LEVEL: info
      TZ: ${TZ:-America/Santiago}
    volumes:
      - /srv/data/jellyseerr/config:/app/config
    ports:
      - "5055:5055"
    networks:
      - homelab

  # -----------------------------------------------------------
  # Sonarr — Descarga automática de series
  # Puerto: 8989
  # -----------------------------------------------------------
  sonarr:
    image: linuxserver/sonarr:4.0.11
    container_name: sonarr
    restart: unless-stopped
    environment:
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
      TZ: ${TZ:-America/Santiago}
    volumes:
      - /srv/data/sonarr/config:/config
      - /srv/media/series:/data/series
      - /srv/media/downloads:/data/downloads
    ports:
      - "127.0.0.1:8989:8989"
    networks:
      - homelab

  # -----------------------------------------------------------
  # Radarr — Descarga automática de películas
  # Puerto: 7878
  # Fix conocido: deshabilitar IPv6 si falla la conexión
  # -----------------------------------------------------------
  radarr:
    image: linuxserver/radarr:5.17.2
    container_name: radarr
    restart: unless-stopped
    environment:
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
      TZ: ${TZ:-America/Santiago}
    # Deshabilitar IPv6 si el upstream IPv6 está roto
    sysctls:
      net.ipv6.conf.all.disable_ipv6: 1
    volumes:
      - /srv/data/radarr/config:/config
      - /srv/media/movies:/data/movies
      - /srv/media/downloads:/data/downloads
    ports:
      - "127.0.0.1:7878:7878"
    networks:
      - homelab

  # -----------------------------------------------------------
  # Prowlarr — Indexador central para Sonarr/Radarr
  # Puerto: 9696
  # -----------------------------------------------------------
  prowlarr:
    image: linuxserver/prowlarr:1.28.2
    container_name: prowlarr
    restart: unless-stopped
    environment:
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
      TZ: ${TZ:-America/Santiago}
    volumes:
      - /srv/data/prowlarr/config:/config
    ports:
      - "127.0.0.1:9696:9696"
    networks:
      - homelab

  # -----------------------------------------------------------
  # Bazarr — Descarga automática de subtítulos
  # Puerto: 6767
  # -----------------------------------------------------------
  bazarr:
    image: linuxserver/bazarr:1.5.1
    container_name: bazarr
    restart: unless-stopped
    environment:
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
      TZ: ${TZ:-America/Santiago}
    volumes:
      - /srv/data/bazarr/config:/config
      - /srv/media/movies:/data/movies
      - /srv/media/series:/data/series
    ports:
      - "127.0.0.1:6767:6767"
    networks:
      - homelab

  # -----------------------------------------------------------
  # qBittorrent — Cliente BitTorrent
  # UI local: http://127.0.0.1:8081
  # -----------------------------------------------------------
  qbittorrent:
    image: linuxserver/qbittorrent:5.0.3
    container_name: qbittorrent
    restart: unless-stopped
    environment:
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
      TZ: ${TZ:-America/Santiago}
      WEBUI_PORT: 8080
      TORRENTING_PORT: 51413
    volumes:
      - /srv/data/qbittorrent/config:/config
      - /srv/media/downloads:/data/downloads
    ports:
      - "127.0.0.1:8081:8080"
      - "51413:51413"
      - "51413:51413/udp"
    networks:
      - homelab
